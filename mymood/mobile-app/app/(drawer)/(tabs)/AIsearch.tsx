import { View, Text } from "react-native";

export default function Library() {
    return (
        <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
        <Text style={{ fontSize: 20 }}>AI search</Text>
        </View>
    );
}